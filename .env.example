# Copy to .env and fill the keys
ANTHROPIC_API_KEY=your_anthropic_key_here
GEMINI_API_KEY=your_gemini_key_here

# AWS / S3 for presigned URL input to Gemini
S3_PROFILE=BCRoot
# If unset, the app will create a bucket automatically on first use
S3_BUCKET=
S3_PREFIX=audio
S3_PRESIGN_TTL_SECONDS=3600
S3_DELETE_AFTER=true

# Prefer presigned S3 URLs over SDK upload
GEMINI_INPUT_MODE=presigned

# Pipeline configuration
# Maximum policy review rounds before failing (default: 6)
# MAX_REVIEW_ROUNDS=6
