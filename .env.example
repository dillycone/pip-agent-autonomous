# Copy to .env and fill the keys
ANTHROPIC_API_KEY=your_anthropic_key_here
GEMINI_API_KEY=your_gemini_key_here

# AWS / S3 for presigned URL input to Gemini
S3_PROFILE=BCRoot
# If unset, the app will create a bucket automatically on first use
S3_BUCKET=
S3_PREFIX=audio
S3_PRESIGN_TTL_SECONDS=3600
S3_DELETE_AFTER=true

# Prefer presigned S3 URLs over SDK upload
GEMINI_INPUT_MODE=presigned

# Pipeline configuration
# Maximum policy review rounds before failing (default: 1, max: 1)
# MAX_REVIEW_ROUNDS=1

# PIP Draft Generation Configuration
# Model to use for PIP draft generation
# Options: claude-sonnet-4-5-20250929, claude-haiku-4-5-20251001, gemini-2.5-pro
# Default: claude-sonnet-4-5-20250929
# PIP_DRAFT_MODEL=claude-sonnet-4-5-20250929

# Thinking budget for PIP generation (tokens)
# Higher budget = deeper reasoning, better quality, but higher cost
# Claude models: 1024-128000 tokens (recommended: 64000)
# Gemini 2.5 Pro: 128-32768 tokens (recommended: 16384)
# Set to 0 to disable thinking (not recommended for PIP quality)
# Default: 64000 for Claude, 16384 for Gemini (auto-selected based on model)
# PIP_THINKING_BUDGET=64000

# Temperature for PIP generation (0-1 for Claude, 0-2 for Gemini)
# Lower = more focused, Higher = more creative
# Default: 0.2
# PIP_TEMPERATURE=0.2

# Maximum output tokens for PIP generation
# Default: 4096
# PIP_MAX_OUTPUT_TOKENS=4096

# Transcription Configuration
# Thinking budget for Gemini transcription (tokens)
# Set to minimum (128) to reduce costs - thinking provides no benefit for transcription
# Valid range: 128-32768 tokens
# Default: 128 (minimum)
# GEMINI_TRANSCRIBE_THINKING_BUDGET=128

# Note on Costs (as of January 2025):
# - Claude Sonnet 4.5: $3/MTK input, $15/MTK output (thinking counted as output)
# - Claude Haiku 4.5: $1/MTK input, $5/MTK output (thinking counted as output)
# - Gemini 2.5 Pro: $1.25/MTK input, $10/MTK output for ≤200k tokens
#                   $2.50/MTK input, $15/MTK output for >200k tokens
#                   (thinking counted as output)
